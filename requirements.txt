# Web Search & Analysis
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
httpx>=0.27.0

# Domain Intelligence (required for deep audit / Sales Triggers)
python-whois>=0.8.0
dnspython>=2.4.0
# Note: wappalyzergo binary is used for tech detection (via subprocess)

# Domain Intelligence (Sales Triggers)
python-whois>=0.8.0
dnspython>=2.4.0

# Browser Automation (Contact Extraction)
playwright>=1.40.0
# Note: Run 'playwright install chromium' after pip install

# AI/LLM
google-generativeai>=0.3.0

# Async support
aiohttp>=3.9.0
asyncio-throttle>=1.0.2

# Web Interface
flask>=3.0.0
flask-cors>=4.0.0

# Utils
python-dotenv>=1.0.0
rich>=13.7.0
pydantic>=2.5.0
psutil>=5.9.0

# AI Fallback Chain (providers 0-3 usan requests, ya incluido arriba)
# Provider 4 — WebLLM local (opcional, solo necesario si todos los cloud fallan)
# llama-cpp-python>=0.2.0       ← instalar manualmente si se necesita offline
# huggingface-hub>=0.20.0       ← para descarga automática del modelo GGUF

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
